/*#include <stdio.h>

int main() {

// //Faça umm progama em c que leia um vetor de 8 posições e, em seguida, leia também dois valores x e y quaisquer correspondentes a duas posições no vetor. Ao final seu prorama deverá escrever a soma dos valores encontrados nas respectivas posições X e Y.

    int vetor[8];
    int x, y;

    printf("Digite 8 valores para o vetor:\n");
    for (int i = 0; i < 8; i++) {
        scanf("%d", &vetor[i]);
    }

    
    printf("Digite dois valores para x e y (posições no vetor, de 0 a 7):\n");
    scanf("%d %d", &x, &y);

    if (x >= 0 && x < 8 && y >= 0 && y < 8) {
        int soma = vetor[x] + vetor[y];
        printf("A soma dos valores nas posições %d e %d é: %d\n", x, y, soma);
    }

}*/