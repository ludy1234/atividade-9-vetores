/*#include <stdio.h>

int main() {
  //Faça um programa que receba do usuario um vetor com 10 posições. Em seguida
deverá ser impresso o maior e o menor vetor.

  int vetor[10];

  printf("Digite 10 numeros inteiros: ");
   for (int i = 0; i < 10; i++) {
        scanf("%d", &vetor[i]);
    }

  int maior = vetor [0];
  int menor = vetor [0];

     for (int i = 0; i < 10; i++) {
       if (vetor[i] > maior){
         maior = vetor[i];
        }
       if (vetor[i] < menor){
         menor = vetor[i];
        }
   }

  printf("O maior vetor é %d\n", maior);
  printf("O menor vetor é %d\n", menor);

}*/