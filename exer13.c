/*#include <stdio.h>

int main() {
  //Fazer um progama para ler 5 valores e, em seguida mostrar a posição onde se encontram  o maior e o menor valor.

  int valores[5], maior, menor, pMaior = 0, pMenor = 0;

  for (int i = 0; i < 5; i++) {
    printf("Digite um numero: ");
        scanf("%d", &valores[i]);
}
  maior = valores[0];
  menor = valores[0];

  
     for (int i = 0; i < 5; i++) {
       if (valores[i] > maior){
         maior = valores[i];
         pMaior = i;
       }
       if (valores[i] < menor){
         menor = valores[i];
         pMenor = i;
        }
}
  printf("Maior: %d na posição %d. Menor: %d na posição %d", maior, pMaior, menor, pMenor);

}*/
