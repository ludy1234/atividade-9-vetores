/*#include <stdio.h>

int main() {
  //Escreva um progama que leia 10 numeros inteiros e os armazene em um vetor. Imprima o vetor, o maior elemento e a posição que ele se encontra.

    int vetor[10];
    int maior, posicaoMaior;

    printf("Digite 10 números inteiros: \n");
    for (int i = 0; i < 10; i++) {
        scanf("%d", &vetor[i]);
    }

    maior = vetor[0];
    posicaoMaior = 0;

    for (int i = 1; i < 10; i++) {
        if (vetor[i] > maior) {
            maior = vetor[i];
            posicaoMaior = i;
        }
    }

    printf("Vetor: ");
    for (int i = 0; i < 10; i++) {
        printf("\n%d ", vetor[i]);
    }

    printf("O maior elemento no vetor é: %d\n", maior);
    printf("Ele está na posição: %d\n", posicaoMaior);
  
}*/